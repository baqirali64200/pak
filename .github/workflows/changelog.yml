const fs = require('fs'); // Import the file system module

// Define the options for the git logger
const options = {
  author: 'baqirali64200@gmail.com',
  since: '1d' // Limit it to last day
};

// Simulated function to get Git commit logs
function getGitCommitLogs(options) {
  // This function is simulated for demonstration purposes
  // You should replace it with your actual logic to fetch Git commit logs
  return [
    { message: 'First commit', author: 'John Doe', date: '2024-02-26' },
    { message: 'Second commit', author: 'Jane Smith', date: '2024-02-25' }
    // Add more commit logs here as needed
  ];
}

// Get the commits and other info
const log = getGitCommitLogs(options);

// Write the commit log data to a JSON file
fs.writeFileSync("changelog.json", JSON.stringify(log));
